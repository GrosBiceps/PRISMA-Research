# ============================================================
# requirements.txt — FlowSOM MRD Pipeline (CPU / Mode de base)
# Python 3.11+
#
# Ce fichier contient UNIQUEMENT les dépendances nécessaires
# pour faire fonctionner l'application en mode CPU.
#
# Pour les fonctionnalités GPU et scRNA-seq optionnelles :
#   pip install -r requirements-gpu.txt
#
# Pour une installation complète (CPU + GPU + notebooks) :
#   pip install -e ".[full,gpu]"
# ============================================================

# ── Cytométrie / FlowSOM (CRITIQUE) ─────────────────────────
flowsom==0.2.2               # algorithme principal (fs.FlowSOM, fs.metaclustering)
flowio==1.4.0                # lecture/écriture FCS natif (FlowIO)
fcswrite==0.6.4              # export FCS avec colonnes de clustering
pytometry==0.1.6             # transformations logicle/arcsinh, prétraitement
umap-learn==0.5.11           # réduction dimensionnelle UMAP
openTSNE>=1.0.0              # t-SNE multithread rapide (viSNE) — fallback sklearn si absent
phate>=1.0.0                 # PHATE trajectoires biologiques — fallback PCA si absent
anndata==0.12.7              # structure de données cellules × marqueurs
mudata>=0.3.0                # conteneur multi-modalité (FlowSOM interne)

# ── Clustering avancé ────────────────────────────────────────
phenograph>=1.5.7            # PhenoGraph : KNN graph + détection communautés Louvain/Leiden
hdbscan>=0.8.0               # HDBSCAN : clustering densité hiérarchique
# parc : nécessite hnswlib (compilation C++ MSVC/MinGW requise)
#        pip install hnswlib  puis  pip install parc
#        Fallback automatique : AgglomerativeClustering si absent

# ── Calcul scientifique ──────────────────────────────────────
numpy>=1.26,<2.0             # 1.x requis par numba ; éviter numpy 2.x
pandas>=2.0                  # manipulation tabulaire
scipy==1.13.0                # stats (Mann-Whitney, KS, cdist), linalg.pinv
scikit-learn==1.8.0          # silhouette, AgglomerativeClustering, MinCovDet
numba>=0.58                  # JIT du SOM CPU (FlowSOM interne, _som.py)
igraph==1.0.0                # MST pour le SOM grid (FlowSOM interne)
networkx>=3.0                # graphes

# ── Interface et visualisation ───────────────────────────────
matplotlib>=3.7
seaborn==0.13.2
reportlab==4.4.9             # génération rapports PDF
kaleido>=0.1.0               # export statique des figures Plotly
plotly>=5.0
PyQt5==5.15.11
PyQtWebEngine==5.15.7        # visualisations interactives (figures HTML)
qtawesome>=1.3.0             # icônes vectorielles Font Awesome 5 (GUI Wizard)

# ── Monitoring de performance système ────────────────────────
psutil>=5.9                  # collecte CPU / RAM / Disque / Réseau en temps réel
# GPUtil — métriques GPU NVIDIA : dans requirements-gpu.txt uniquement

# ── Configuration ────────────────────────────────────────────
PyYAML==6.0.2

# ── Logging ──────────────────────────────────────────────────
loguru>=0.7
